# lista004-JavaScript
O loop do-while é semelhante ao while, mas a condição é testada após a primeira execução do bloco de código. Isso garante que o código dentro do loop será executado pelo menos uma vez.
